OPEN BUGS
---------
To work on these problems and build tests you need to know that php.ini is loading both ACE libs.  OpenADs is 
working on port 8080 as fastcgi and SAP ADS is responding on port 80 also as fastcgi.

If you wish to work with Python, you should be able to use f:\python_ads for SAP ACE.  SAP libs are in F:\Ads11.
To use python with OpenAds you can use OpenAds Python ext found in: f:\OpenAds\bindings\python.

We have written a tool to replace SAP Data Architect named DaWeb.  It is found in f:\OpenAds\DA-Web.

Use the REMOTE server for SAP as well as OpenADS for your tests. SAP ADS is running as a service and responding on port 6262.
To load OpenAds as a service read readme.md where you can find detailed instructions on how.  I suggest you make it respond on por 16262.

Use DD found in f:\Openads\testdata\pmsys\pmsys.add with SAP ADS and use f:\Openads\testdata\pmsys\pmsys_imported.add with OpenAds. 
Adssys user password on both dd is pmsys.

pmsys_imported was imported using newly created tool in f:\Openads\Tools\Import_dd.  In essense we 
endup with a new DD that is compatible with our OpenAds not with SAP.  Import only needs to happen once.  If you have doubts
how to use this tool and what it does look at readme.md on f:\Openads\tools\import_dd

If you find that the Python extensin is faulty, fix it.  If you find import tool is faulty, fix it.  If there are problems with OpenADS core
functions, fix it.  We have all sources and build tools. 

I remind you that you can test what output should look like by connecting with SAP ADS ot pmays.add comparing results with 
those after connecting with OpenADS to pmsys_imported.add.  Our objective is make OpenAds as fast and complete as SAP ADS. 

Below is what I want you to work on:
1.  [DONE] SQL editor in DAWeb needs to color reserved words and comments in different colors.  Likewise, color function names differently.  Follow
color scheme used by HeidiSQL. In ADS SQL comments are marked as in C.  /* this is a comment */  Comments are also anything following a double
forward slash //
Implemented: js/ads-sql-mode.js — ace/mode/ads_sql (keywords, functions, types, // and /* */ comments)
             ace/theme/heidisql — light HeidiSQL-like colours (blue keywords, purple functions, green comments, crimson strings)

2. [DONE] On DaWeb, when viewing triggers of a table, the tabulator on the right shows the triggers and the editor on the bottom shows
the trigger body.  Among the trigger columns on the tabulator grid, you have the column "timing".  It is empty on all triggers.
Table properties on Pmsys.add shows these columns on file:  file:///C:/Users/ADMINI~1/AppData/Local/Temp/2/exp30E1.htm   Column timing on
the trigger grid should be populated and it is not.
Fixed: patched pmsys_imported.am JSON blocks with correct timing from SAP system.triggers; import_dd tool now captures timing in future imports.


3.  I think I need to explain how 3 special tables on triggers work.  Make sure OpenAds triggers works like SAP ADS: 
Three special in-memory tables are used by triggers; __old, __new, and __error. These tables can be referenced using just 
their base names (i.e., __new). For example, to read a value from the __new table the following SQL statement can be executed:
SELECT empid FROM __new
The __new table is used when firing an INSERT or UPDATE trigger. It contains one row with the same field definitions as the 
base table on which the update was performed. The field values are the values that are about to be (in case of a BEFORE, or INSTEAD OF, 
or CONFLICT trigger) or were already (in case of an AFTER trigger) written to the table.  The __old table is used when firing an 
UPDATE or DELETE trigger. It contains one row with the same field definitions as the base table on which the update was performed. 
The field values are the values that used to be in the table (immediately before the current update was performed).  The __error table is 
used to return an error code and/or error string from inside of a trigger. The __error table has the following table definition:

INTEGER
 errno
 
MEMO
 message
 

If trigger code writes a row to this table, the Advantage server will read the error number and message from the __error table and return 
them to the client as a native exception or error code (depending on which client you are using). The errno field is optional, and 
if left empty, a default trigger failure error code will be returned to the client.

The user can make changes to the data values in the __new and __old tables but these changes will not be reflected in the record 
update or insert operation. To make changes to the values provided by the client, use an INSTEAD OF trigger, change any data values 
in the __new table you wish to modify, then post the __new record manually. The example below is an INSTEAD OF INSERT trigger that modifies 
the date_inserted field and puts the current date and time into that field:

UPDATE __new SET date_inserted = (SELECT now() FROM system.iota);

INSERT INTO mytable SELECT * FROM __new;






OPEN FEATURES
-------------

4. [MEDIUM] Inline table editing in Tabulator.
   Users need to edit non-PK cell values directly in the data grid.
   A Save button should commit changes via row_ops.php.
   PK columns must remain read-only.
   Files: js/app.js, api/row_ops.php

5. [LOW] Studio AOF features in DA-Web.
   Review Studio web console's AOF/Rushmore demo flow.
   Identify AOF features (filter install, OptLevel display, CREATE INDEX hint chips)
   that can be adapted into DA-Web's table browser.


OPEN MAINTENANCE
----------------

6. openads_serverd smart DLL detection.
   Server should detect whether ace64.dll or openace64.dll is present and
   load whichever is available as long as ACE64 is not the SAP version.

7. Cross-platform build verification.
   Confirm full source tree builds on Linux and macOS.  Run unit tests on
   each platform and document results.


8. wire-protocol.md — add ALL DD functions and usage.

9. More tests for DD: permissions enforcement, triggers firing, RI rules.

10. Inspect Studio web console of OpenADS and see how AOFs work; adapt
    useful features into daWeb when browsing a table.

14. OpenADS needs to build cross-platform. See git repo CI for current
    failures on non-Windows platforms.


COMPLETED
---------


DD PERMISSIONS IMPLEMENTATION STATUS (2026-06-03)
--------------------------------------------------

system.permissions virtual table is implemented and returns correct data for
OpenADS-created .add files.  For SAP-created .add files (e.g. pmsys.add)
there are two known gaps:

ROOT CAUSE 1 — ENCRYPTED BLOBS (not yet fixed)
  SAP writes 0x80000000 in info2 for ALL Permission records regardless of
  actual rights.  The real per-table per-right bits (READ/UPDATE/INSERT/DELETE)
  are stored in two 8-byte opaque encrypted blobs in the property zone.
  OpenADS treats the sentinel as "full DML" for all groups.
  Impact: General group shows full DML on every table; SAP shows read-only on
  some (sys_registry, tags, autoreports, etc.).

  Property zone layout (SAP Permission record):
    prop[0..1]:  08 00  (length=8)
    prop[2..9]:  [8 bytes encrypted blob 1]  <- ADS_PERMISSION bitmask
    prop[10..11]: FF FF
    prop[12..13]: 08 00  (length=8)
    prop[14..21]: [8 bytes encrypted blob 2]  <- WITH GRANT version
    prop[22+]:   FF FF padding

  The encryption is object-specific (same bitmask → different blobs for
  different tables).  Encryption algorithm is inside ACE binary only.
  SAP Data Architect has NO decryption code — it just calls system.permissions.

  TO FIX: Either reverse-engineer the ACE binary cipher, OR implement a
  hybrid mode: when ACE64.dll is present, call AdsDDGetPermissions() to
  populate permissions_ with correct bitmasks during .add binary load.
  See tests/tools/sap_perm_probe.cpp for how to call AdsDDGetPermissions().

ROOT CAUSE 2 — MISSING ZERO-ROWS (fixable)
  SAP system.permissions emits a row for EVERY (grantee × object) combination,
  showing zeros for ungranted permissions.
  OpenADS only emits rows where a Permission record actually exists.
  Impact: 40 missing rows per grantee (no-grant objects silently absent).

  TO FIX in src/abi/ace_exports.cpp (~line 6985 system.permissions handler):
  After iterating dd->permissions(), enumerate all grantees × all objects and
  emit a zero-row for each (grantee, object) pair without an ACL entry.

ROOT CAUSE 3 — EXTRA FIELD ROWS (fixable)
  ('row', field, auditlog) and ('seq', field, propertytransactions) appear in
  OpenADS output but not in SAP — these are DBF fields not registered in the DD.
  TO FIX: filter field rows against the DD field list.
