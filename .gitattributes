* text=auto eol=lf
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf
*.png   binary
*.jpg   binary
*.gif   binary
*.dbf   binary
*.cdx   binary
*.ntx   binary
*.adt   binary
*.adi   binary
*.adm   binary
*.fpt   binary
*.dbt   binary

# Exclude internal AI-scaffold planning notes from `git archive`
# (and therefore from the GitHub-Release auto-attached
# "Source code (zip/tar.gz)" tarballs). They stay in the repo
# for local reference but never ship to consumers.
docs/superpowers/ export-ignore
build/            export-ignore
build-*/          export-ignore
